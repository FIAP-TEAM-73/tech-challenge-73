apiVersion: v1
kind: ConfigMap
metadata:
  name: db-config
  labels:
    name: db-config
data:
  DB_NAME: database_fastfood
  DB_PORT: "5432"
  PGSSLMODE: no-verify
  ORDER_API_HOST: http://svc-order-api/api/v1
